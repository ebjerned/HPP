#include <stdio.h>
#include <assert.h>

int main(){
	
	int a, b;
	printf("Enter two numbers: ");
	scanf("%i %i", &a, &b);

	printf("%i", a);
	
/*	if (a > 0) printf("Yes");
	if((a%2 == 0) && (b%2==0)){
		int result = a + b;
		printf("%i", result);
	}else{
		int result = a * b;
		printf("%i", result);
	}
*/


	return 0;
}
